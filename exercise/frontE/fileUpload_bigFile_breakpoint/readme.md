es6文件对象、ajax上传、asycn await promise、后台文件存储、流操作全栈技能。全面解决问题的能力和技术细节

文件上传是开发中的难点，大文件上传及断点续传是其中的难点和核心技术点。


大文件和断点续传    

大文件 
切片
1. js 在es6 文件对象 file，node file stream 文件处理能力有所增强
分割成blob类型
start，size，offset
http 请求 同时上传多个切片，并发上传，速度更快，改善体验。

- 前端的切片，让http并发上传，使大文件上传速度更快
    1. file.slice 完成切片，blob类型文件切片 （js中的二进制文件类型） blob协议 预览上传的文件 。
- 服务器端
    如何将这些切片，合并成一个，并且能显示原来的图片
    stream 流
    可读流 可写流
    chunk 都是二进制文件按
    promise.all 来包装每个chunk的写入
    start end  
    每个 chunk写入的过程
     先创建可读流，再pipe给可写流的过程

    医院文件作为文件夹的名字，再上传blobs到此文件夹，每个blob 都已文件名-index的命名方式来存储


- 文件上传
    无论是前端还是后端， 传输文件 特别是大文件，可能会发生文件丢失的情况，网速， 服务器超时， 如何避免丢失
    hash，文件并不是唯一。 
    针对文件内容进行hash计算，
    匹配前后端hash
    一样 则传输成功， 若不一样则重传。

- html5 特性 如何理解， localStorage...
    Web Workers 可用于优化我们的前端性能。将要花费大量时间的，复杂的，启用新线程去进行计算。例如大文件上传中的hash 的计算
- es6 那些特性，你怎么使用
    函数使用默认参数

- 给用户实时的反馈，是提升用户体验的核心